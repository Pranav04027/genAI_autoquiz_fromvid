import express from "express"
import cors from "cors"
import path from "path"
import {  }

const app = express()
app.use(express.json())
app.use(cors())

app.post

app.listen(8000, () => {
    console.log("Whisper Server connected at Port 8000")
})
